/*
    The entry point file must have the following functions.

    setup: run once when creating a game, used to set up state.
    save: run once, right before a game is saved to disk.
    load: run once whenever a saved game is loaded.
    main: run every tick of the game after main.
*/

function setup()

end

function save()

end

function load()

end

function main()

end